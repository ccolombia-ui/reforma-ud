name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - 'content/drive-sync/**'   # sync commits no disparan CI completo
      - 'docs/**'
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      # v5.0d fix · pnpm version la lee del packageManager del package.json
      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm --filter portal-next lint || true   # no bloquear en fase inicial

      - name: Build portal-next
        run: pnpm --filter portal-next build

      - name: Upload build artifact
        uses: actions/upload-artifact@v4
        with:
          name: portal-next-dist
          path: apps/portal-next/.next/
          retention-days: 7
