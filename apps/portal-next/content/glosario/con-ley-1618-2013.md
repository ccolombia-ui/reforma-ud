---
fileClass: fc-concepto-universal
kd_id: urn:aleia:udfjc:reforma:cap-mi12:con:ley-1618-2013
kd_title: "Ley 1618 de 2013 — Estatutaria de derechos personas con discapacidad"
kd_type: glosario-universal
kd_status: APPROVED
kd_version: v1.0.0
kd__up: "[[con-tipo-normativo]]"


skos_prefLabel: "Ley 1618 de 2013 · Estatutaria personas con discapacidad"
skos_altLabel:
  - "Ley 1618/2013"
  - "Ley Estatutaria 1618"
  - "Ley discapacidad Colombia"

skos_definition: "Ley estatutaria de la República de Colombia, expedida el 27 de febrero de 2013, **'por medio de la cual se establecen las disposiciones para garantizar el pleno ejercicio de los derechos de las personas con discapacidad'**. Como ley estatutaria (de las pocas que regulan derechos fundamentales), se ubica en jerarquía normativa **inmediatamente debajo de la Constitución de 1991** y por encima de leyes ordinarias. Operacionaliza la Convención de Naciones Unidas sobre los Derechos de las Personas con Discapacidad (ratificada por Colombia mediante Ley 1346 de 2009). Su Art. 11 demanda al sector educativo reglamentar aspectos de educación inclusiva — mandato directo que da origen al Decreto 1421/2017. Establece principios de inclusión, ajustes razonables, accesibilidad y diseño universal aplicables a todos los sectores (educación, salud, trabajo, cultura, transporte, etc.)."
skos_scopeNote: "ESTE es el fundamento legal estatutario de la cadena que culmina en obligación de UDL para IES colombianas: Convención ONU → Ley 1346/2009 (ratificación) → Ley 1618/2013 (estatutaria) → Decreto 1421/2017 (reglamentación educación inclusiva) → Decreto 1330/2019 (registro calificado IES con principios inclusión) → ACU-004-25 (carta magna UDFJC). El Art. 11 es el punto de inflexión normativo."
skos_example: "El sustento jurídico de UDL en UDFJC inicia en la Ley 1618/2013 Art. 11, que obliga al MEN a reglamentar educación inclusiva — el Decreto 1421/2017 es esa reglamentación + adopta DUA/UDL — UDFJC debe cumplirlo para conservar registro calificado."
skos_notation: "Ley 1618/2013"

iso_designation_type: term
iso_definition_type: intensional
iso_genus: "Ley estatutaria de la República de Colombia"
iso_differentia: "Garantiza derechos personas con discapacidad · operacionaliza Convención ONU · jerarquía estatutaria · fundamento del Decreto 1421/2017"
iso_subject_field: "Derecho constitucional colombiano · Derechos humanos · Discapacidad"
iso_term_status: preferred
iso_standardized_by: "Congreso de la República de Colombia (Ley Estatutaria 1618 del 27 de febrero de 2013)"

pasteur_quadrant: EDISON







rol_seleccionado: estudiante-soberano



cited_in:
  - "[[sec-MI12-01--mandato-normativo]]"
  - "[[sec-MI12-03--estandares-internacionales]]"
cited_count: 2

tags:
  - glosario-universal
  - concepto-normativo
  - t1-normativo
  - ley-estatutaria
  - ley-1618-2013
  - inclusion-discapacidad
  - m01-corpus
  - audit-v2
  - tpl-v2
---


# `INPUT[text(class(meta-bind-readonly)):skos_prefLabel]`

> [!quote]+ ⚖️ Ley estatutaria · fundamento del derecho a UDL en Colombia
> Ley estatutaria que garantiza el pleno ejercicio de los derechos de las personas con discapacidad. Su **Art. 11** demanda al sector educativo reglamentar la educación inclusiva — mandato directo que da origen al **Decreto 1421/2017** (que adopta UDL/DUA como obligatorio para IES). Operacionaliza la Convención ONU 2006 (ratificada por Ley 1346/2009).

---

## §0 · 🎭 Vista por rol institucional

`INPUT[inlineSelect(option(estudiante-soberano,🎓 Estudiante Soberano), option(docente-disenador,🎨 Diseñador), option(docente-formador,🎤 Formador), option(docente-investigador-pasteur,🔬 Investigador Pasteur), option(docente-emprendedor-coop,🤝 Emprendedor/Coop), option(docente-director,🏛️ Director)):rol_seleccionado]`

---

## §1 · Definición canónica

> `INPUT[textArea(class(meta-bind-readonly)):skos_definition]`

| Sub-tipo | Pasteur | Authority level |
|---|:-:|:-:|
| `INPUT[text(class(meta-bind-readonly)):concept_subtype]` | `INPUT[text(class(meta-bind-readonly)):pasteur_quadrant]` | `INPUT[text(class(meta-bind-readonly)):concepto_facet_normative.normative_authority_level]` |

## §2 · 📜 Anclaje normativo + cadena de adopción

```dataviewjs
const me = dv.current();
const f = me.concepto_facet_normative;
if (!f) {
  dv.paragraph("(sin facet normative)");
} else {
  dv.table(["Campo", "Valor"], [
    ["**Origen**", `${f.origin_type ?? "—"} · fuerza original: ${f.origin_force ?? "—"}`],
    ["**Authority level**", f.normative_authority_level ?? "—"],
    ["**Effective force en UDFJC**", f.effective_force_in_udfjc ?? "—"],
    ["**Chain status**", f.chain_status ?? "—"]
  ]);

  const chain = f.adoption_chain ?? [];
  if (chain.length) {
    dv.header(4, `🔗 Cadena de adopción · ${chain.length} eslabón(es)`);
    dv.table(["Adoptante", "Locator", "Autoridad", "Fecha", "Evidencia"],
      chain.map(a => [a.adopter, a.adopter_locator, a.adopter_authority_level, a.adopted_at, a.adoption_evidence]));
  }
}
```

## §3 · 🔻 Pre-requisitos cognitivos

```dataviewjs
const prereq = dv.current().concepto_prerequisitos ?? [];
if (prereq.length === 0) dv.paragraph("Sin pre-requisitos formales declarados.");
else dv.list(prereq);
```

## §4 · 🔺 Conceptos que declaran este como pre-requisito

```dataviewjs
const me = dv.current();
const here = me.file.name;
const folder = me.file.folder;
const all = dv.pages(`"${folder}"`).where(p => p.kd_type === "glosario-universal");

const matchHere = (target) => {
  if (!target) return false;
  if (typeof target === "object" && target.path !== undefined) {
    return String(target.path).split("/").pop().replace(/\.md$/, "").trim() === here;
  }
  const s = String(target);
  const m = s.match(/\[\[([^\]|]+?)(?:\|[^\]]*)?\]\]/);
  if (m) return m[1].split("/").pop().replace(/\.md$/, "").trim() === here;
  return s.split("/").pop().replace(/\.md$/, "").trim() === here;
};

const habilitados = all.where(p => (p.concepto_prerequisitos ?? []).some(matchHere)).array();
dv.header(4, `📚 ${habilitados.length} concepto(s) declaran este como pre-requisito directo`);
if (habilitados.length === 0) dv.paragraph("_Sin reverse declaraciones todavía._");
else dv.list(habilitados.map(p => p.file.link));
```

## §5 · 📋 Mandatos derivados

```dataviewjs
const mandates = (dv.current().tupla__relations ?? []).filter(r => r.rel_nombre === "norm_mandates");
if (mandates.length === 0) dv.paragraph("Sin mandatos derivados.");
else dv.table(["§", "Mandato", "Evidencia"],
  mandates.map((r, i) => [`§${i+1}`, r.rel_target, r.rel_propiedades?.norm_evidence ?? "—"]));
```

## §7 · 🤝 Relaciones tipadas (outgoing)

```dataviewjs
const me = dv.current();
const rels = me.tupla__relations ?? [];
const vocabPage = dv.page("00-glosoario-universal/_vocabulario-relaciones");
const relMap = vocabPage?.relaciones ?? {};
const frameMap = vocabPage?.frames ?? {};

const lookupRel = (n, d) => {
  const dir = d ?? "co";
  return relMap[n]?.[dir] ?? relMap[n]?.co ?? relMap[n]?.pre ?? relMap[n]?.post ?? null;
};
const humanLabel = (n, d) => lookupRel(n, d)?.label ?? `\`${n}\``;
const humanDesc = (n, d) => lookupRel(n, d)?.description ?? "—";
const humanFrame = (f) => frameMap[f ?? "general"]?.label ?? `\`${f ?? "general"}\``;

const groups = {};
for (const r of rels) {
  if (r.rel_nombre === "norm_mandates") continue;
  const k = r.rel_frame ?? "general";
  groups[k] = groups[k] ?? [];
  groups[k].push(r);
}

for (const [frame, rs] of Object.entries(groups)) {
  dv.header(4, `${humanFrame(frame)} · ${rs.length} relación(es)  \`[frame: ${frame}]\``);
  const byRel = {};
  for (const r of rs) {
    const key = `${r.rel_nombre}::${r.rel_direccion ?? "co"}`;
    byRel[key] = byRel[key] ?? { rel_nombre: r.rel_nombre, rel_direccion: r.rel_direccion, items: [] };
    byRel[key].items.push(r);
  }
  for (const grp of Object.values(byRel)) {
    dv.header(5, `${humanLabel(grp.rel_nombre, grp.rel_direccion)}  \`(${grp.rel_nombre} · ${grp.rel_direccion ?? "co"})\``);
    dv.paragraph(`> ${humanDesc(grp.rel_nombre, grp.rel_direccion)}`);
    dv.table(["→ Target", "Evidencia"],
      grp.items.map(r => [r.rel_target, r.rel_propiedades?.norm_evidence ?? r.rel_propiedades?.skos_evidence ?? "—"]));
  }
}
```

## §10 · 📜 Citado en

```dataviewjs
const me = dv.current();
dv.list(me.cited_in ?? []);
dv.paragraph(`**Total citaciones**: ${me.cited_count ?? 0}`);
```

---

## Historial

| Versión | Fecha | Cambios |
|---|---|---|
| **v1.0.0** | **2026-04-27** | Concepto creado en PRE-Sprint 1A (cadena de adopción UDL). Modela Ley 1618/2013 como **fundamento legal estatutario** del Decreto 1421/2017 que adopta UDL para IES colombianas. |

---

*CC BY-SA 4.0 · UDFJC · 2026-04-27 · `con-ley-1618-2013` v1.0.0 · TPL T1 NORMATIVO*
